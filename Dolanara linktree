<!doctype html>
<html lang="id">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Dolanara — Link Bio</title>

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500&family=Poppins:wght@600;700&display=swap" rel="stylesheet">

  <style>
    :root{
      --bg-grad-1: #E9F8F1; /* krem-hijau very light */
      --bg-grad-2: #D9F0FF; /* blue-sky tint */
      --accent: #2F6B3B; /* hijau daun */
      --muted: #6B6B6B;
      --btn-radius: 14px;
      --max-width: 760px;
      --glass-blur: 8px;
    }

    /* Reset */
    *{box-sizing:border-box;margin:0;padding:0}
    html,body{height:100%}
    body{
      font-family: "DM Sans", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background: linear-gradient(135deg, var(--bg-grad-1), var(--bg-grad-2));
      color:#0b1720;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:32px;
    }

    /* Page container */
    .wrap{
      width:100%;
      max-width:var(--max-width);
      min-height:420px;
      border-radius:20px;
      overflow:hidden;
      position:relative;
      box-shadow: 0 10px 30px rgba(6,20,30,0.12);
      background: linear-gradient(180deg, rgba(255,255,255,0.75), rgba(255,255,255,0.65));
      backdrop-filter: blur(var(--glass-blur));
    }

    /* Background image overlay */
    .bg-photo{
      position:absolute;
      inset:0;
      z-index:0;
      background-position:center;
      background-size:cover;
      background-repeat:no-repeat;
      opacity:0.22; /* subtle */
      filter: blur(6px) saturate(0.95);
      mix-blend-mode: normal;
      transition:opacity .4s ease;
    }

    /* Gradient overlay to ensure readability */
    .bg-overlay{
      position:absolute;
      inset:0;
      z-index:1;
      background: linear-gradient(180deg, rgba(255,255,255,0.35), rgba(255,255,255,0.6));
    }

    /* Content area */
    .content{
      position:relative;
      z-index:2;
      padding:28px;
      display:flex;
      gap:20px;
      align-items:center;
      flex-direction:column;
      text-align:center;
    }

    /* Logo */
    .logo-wrap{
      width:120px;
      height:120px;
      border-radius:20px;
      overflow:hidden;
      display:flex;
      align-items:center;
      justify-content:center;
      background:linear-gradient(135deg, rgba(255,255,255,0.6), rgba(255,255,255,0.4));
      box-shadow: 0 6px 18px rgba(15,30,20,0.06);
      transition:transform .25s ease;
    }
    .logo-wrap img{
      max-width:88%;
      max-height:88%;
      object-fit:contain;
      display:block;
    }
    .logo-wrap.missing{
      font-family: "Poppins", sans-serif;
      color:var(--muted);
      font-weight:600;
      font-size:14px;
      padding:12px;
    }

    .brand-name{
      font-family: "Poppins", sans-serif;
      font-size:20px;
      color:var(--accent);
      margin-top:4px;
      letter-spacing:0.2px;
    }
    .brand-sub{
      font-size:13px;
      color:var(--muted);
      margin-top:6px;
    }

    /* Links/buttons */
    .links{
      width:100%;
      display:flex;
      flex-direction:column;
      gap:12px;
      padding: 8px 6px 28px;
      align-items:center;
    }
    .btn{
      width:100%;
      max-width:560px;
      padding:14px 18px;
      background:linear-gradient(90deg, rgba(255,255,255,0.95), rgba(255,255,255,0.85));
      border-radius:var(--btn-radius);
      border:1px solid rgba(10,10,10,0.04);
      font-family: "Poppins", sans-serif;
      font-weight:600;
      color:#0b1720;
      text-decoration:none;
      display:inline-flex;
      align-items:center;
      justify-content:space-between;
      box-shadow: 0 6px 18px rgba(15,30,20,0.04);
      transition: transform .22s ease, box-shadow .22s ease, background .22s ease;
      overflow:hidden;
    }
    .btn .left{
      display:flex;
      gap:12px;
      align-items:center;
    }
    .btn .icon{
      width:40px;height:40px;border-radius:10px;display:flex;align-items:center;justify-content:center;
      background:linear-gradient(180deg, rgba(0,0,0,0.03), rgba(0,0,0,0.02));
      margin-right:4px;font-size:18px;
    }
    .btn small{display:block;font-size:12px;color:var(--muted)}
    .btn:hover{
      transform:translateY(-6px) scale(1.002);
      box-shadow: 0 18px 36px rgba(20,60,30,0.08);
      background: linear-gradient(90deg, rgba(255,255,255,1), rgba(255,255,255,0.9));
    }

    .btn.accent{
      border:1px solid rgba(47,107,59,0.12);
      background: linear-gradient(90deg, rgba(47,107,59,0.06), rgba(47,107,59,0.02));
    }

    /* small helper row for social icons */
    .social-row{
      display:flex;
      gap:12px;
      align-items:center;
      justify-content:center;
      margin-top:6px;
      margin-bottom:6px;
    }
    .social-row a{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      width:44px;height:44px;border-radius:10px;
      text-decoration:none;color:var(--accent);
      background: rgba(47,107,59,0.06);
      border:1px solid rgba(47,107,59,0.06);
      transition:transform .18s ease;
    }
    .social-row a:hover{transform:translateY(-4px)}

    /* small footer note */
    .footer-note{
      font-size:12px;color:var(--muted);margin-top:8px;
    }

    /* Reveal animations */
    .reveal{opacity:0; transform:translateY(10px); transition:opacity .6s ease, transform .6s ease}
    .reveal.visible{opacity:1; transform:none}

    /* Responsive */
    @media (min-width:880px){
      .content{padding:36px}
      .logo-wrap{width:136px;height:136px}
    }
    @media (max-width:420px){
      body{padding:18px}
      .btn{padding:12px}
    }

  </style>
</head>
<body>
  <div class="wrap" id="pageWrap">
    <!-- background image: change assets/bg.jpg if you have a photo -->
    <div class="bg-photo" id="bgPhoto"></div>
    <div class="bg-overlay"></div>

    <div class="content">
      <!-- Logo -->
      <div class="logo-wrap reveal" id="logoWrap">
        <!-- replace src with assets/logo.png -->
        <img src="assets/logo.png" alt="Logo Dolanara" id="logoImg" onerror="logoMissing()" />
      </div>

      <div class="brand-name reveal">Dolanara</div>
      <div class="brand-sub reveal">Staycations • Family Trips • Gathering</div>

      <!-- Links -->
      <div class="links" id="links">
        <a class="btn accent reveal" href="https://www.instagram.com/dolanara_id" target="_blank" rel="noopener noreferrer">
          <span class="left">
            <span class="icon">📸</span>
            <div>
              Instagram
              <small>@dolanara_id</small>
            </div>
          </span>
          <span>Follow</span>
        </a>

        <a class="btn reveal" href="https://www.tiktok.com/@fauzultzy?_t=ZS-90hf9SDnUfP&_r=1" target="_blank" rel="noopener noreferrer">
          <span class="left">
            <span class="icon">🎵</span>
            <div>
              TikTok
              <small>@fauzultzy</small>
            </div>
          </span>
          <span>Watch</span>
        </a>

        <a class="btn reveal" href="https://wa.me/62881027007831" target="_blank" rel="noopener noreferrer">
          <span class="left">
            <span class="icon">💬</span>
            <div>
              Chat via WhatsApp
              <small>0881 0270 07831</small>
            </div>
          </span>
          <span>Chat</span>
        </a>

        <!-- Additional example: booking or website -->
        <a class="btn reveal" href="#" target="_blank" rel="noopener noreferrer" id="bookingBtn">
          <span class="left">
            <span class="icon">📝</span>
            <div>
              Booking & Info
              <small>Reservasi & paket staycation</small>
            </div>
          </span>
          <span>Open</span>
        </a>

        <div class="social-row reveal">
          <a href="https://www.instagram.com/dolanara_id" target="_blank" rel="noopener noreferrer" title="Instagram">IG</a>
          <a href="https://www.tiktok.com/@fauzultzy?_t=ZS-90hf9SDnUfP&_r=1" target="_blank" rel="noopener noreferrer" title="TikTok">TT</a>
          <a href="https://wa.me/62881027007831" target="_blank" rel="noopener noreferrer" title="WhatsApp">WA</a>
        </div>

        <div class="footer-note reveal">Dolanara — membuat momen keluarga menjadi cerita 🌿</div>
      </div>
    </div>
  </div>

  <script>
    // ---------- Configuration: change these if needed ----------
    const bgImagePath = "assets/bg.jpg"; // replace with the family picnic image (optional)
    // ---------------------------------------------------------

    // Set background image if exists
    const bgEl = document.getElementById('bgPhoto');
    fetch(bgImagePath, { method: 'HEAD' })
      .then(r => {
        if (r.ok) {
          bgEl.style.backgroundImage = `url('${bgImagePath}')`;
        } else {
          bgEl.style.opacity = 0; // keep gradient-only if not found
        }
      }).catch(e => {
        bgEl.style.opacity = 0;
      });

    // Logo missing handler: replace with text placeholder so layout stays nice
    function logoMissing(){
      const wrap = document.getElementById('logoWrap');
      wrap.classList.add('missing');
      wrap.innerHTML = '<div style="text-align:center">Logo<br/>Dolanara</div>';
    }

    // Reveal on scroll simple IntersectionObserver
    const reveals = document.querySelectorAll('.reveal');
    const io = new IntersectionObserver((items, obs) => {
      items.forEach(i => {
        if (i.isIntersecting) {
          i.target.classList.add('visible');
          obs.unobserve(i.target);
        }
      });
    }, {threshold: 0.12});
    reveals.forEach(r => io.observe(r));

    // Page fade-in
    document.addEventListener('DOMContentLoaded', () => {
      document.getElementById('pageWrap').style.opacity = 0;
      setTimeout(()=>{ document.getElementById('pageWrap').style.transition = 'opacity .6s ease'; document.getElementById('pageWrap').style.opacity = 1 }, 10);
    });

    // Optional: smooth scroll to top if linking from somewhere
    if ('scrollBehavior' in document.documentElement.style) {
      // supported
    }

    // Accessibility: keyboard focus visible style (basic)
    document.addEventListener('keyup', (e) => {
      if (e.key === 'Tab') document.body.classList.add('user-tabbed');
    });

  </script>
</body>
</html>
